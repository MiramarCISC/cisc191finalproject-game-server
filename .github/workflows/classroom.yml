name: GitHub Classroom Autograding

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build-and-test:
    name: Build and test Spring Boot 3 web client REST JPA/H2 lab
    runs-on: ubuntu-latest

    steps:
      - name: Check out student repository
        uses: actions/checkout@v4

      - name: Set up Eclipse Temurin JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: maven

      - name: Build all modules
        run: mvn -B clean install

      - name: Test Spring Boot 3 REST server module
        run: mvn -B -pl game-server test

      - name: Test Spring Boot web client module
        # Module 3 Test Excluded; GitHub cannot run UI.
        run: |
          mvn -B -pl game-client test '-Dtest=!Module3Test'
